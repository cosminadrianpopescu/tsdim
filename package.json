{
  "name": "tsdim",
  "version": "0.0.2",
  "description": "Simple dependency injection manager without use of constructor and without the need of factories",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "tsc -p ./tsconfig.test.json --diagnostics && cd testing && node ./main.test.js",
    "build": "tsc --diagnostics"
  },
  "author": "Cosmin Popescu",
  "license": "GPL-3.0-or-later",
  "dependencies": {
    "@abraham/reflection": "^0.7.0",
    "reflect-metadata": "^0.1.13"
  },
  "devDependencies": {
    "@types/jasmine": "^3.5.14",
    "@types/node": "^14.10.1",
    "jasmine": "^3.6.1",
    "tslib": "^2.0.1",
    "typescript": "^4.0.2"
  }
}
